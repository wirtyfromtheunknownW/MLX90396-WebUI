MLX90396 Web UIAn industrial-grade, dark-themed WebSerial GUI and diagnostic toolkit for the Melexis MLX90396 3D Hall-effect magnetic sensor.  This application runs directly in any Chromium-based web browser without requiring backend servers, native drivers, or extra software installations. It communicates with an intermediate microcontroller over WebSerial using an asynchronous SCPI command tunneling protocol to execute low-level SPI transfers, manage chip registers, perform 3D joystick calibration, and render real-time vector graphics.  Key Features1. 3D Joystick Visualizer & Emulation3D Perspective Rendering: Uses dynamic CSS variables (--tx, --ty) to translate 3D magnetic position data into a smooth, spring-loaded 3D joystick animation.  28-LED Radial Ring: Features a 360° perimeter ring with 28 virtual LEDs that highlight the current vector angle and magnitude in real-time.  Emulation Mode: Includes a standalone trigonometric wave simulator for UI testing without physical hardware attached.  2. 4-Step Magnetic Calibration WizardStep 1 — Zero Offset Capture: Samples magnetic baseline flux ($B_x, B_y$) at center rest to eliminate mechanical mounting tilt.  Step 2 — Range Sweep Recording: Dynamically records a 360° rotational sweep to find axis spans ($X_{\min}/X_{\max}, Y_{\min}/Y_{\max}$) and normalize non-circular magnetic fields.  Step 3 — Center Deadband Adjust: Interactive slider (0–15%) to snap tiny resting field jitters to zero.  Step 4 — Profile Commit: Instantly applies normalized calibration factors across all telemetry tabs.  Polar Radar Motion Canvas: Live $B_x$ vs. $B_y$ trace canvas with concentric target rings and glowing mint-green vector path tracking.  3. NVRAM / Register Map Editor (0x00 – 0x3F)Full Register Scan (0x00 – 0x3F): Read or write individual 16-bit words or perform a full 64-register memory dump.  Interactive Data Table: Synchronized view showing register addresses, raw hexadecimal values, decimal values, and transfer statuses.  Hardware EEPROM Operations: Dedicated controls for Memory Store (HS) (RAM $\rightarrow$ EEPROM) and Memory Recall (HR) (EEPROM $\rightarrow$ RAM).  4. SCPI Terminal & Low-Latency EngineCommand Queue (Mutex): Prevents packet collisions and stream desynchronization by wrapping requests in individual 1.5-second timeout promises.  Tokenizer Stream Parser: Parses token responses like (OK)>, (ERR)>, (E2BIG)>, and (ERANGE)> in real-time.  Dual Console Logging: High-performance log buffer with requestAnimationFrame DOM batching and automatic memory truncation to prevent browser slowdowns.  Interactive Shell: SCPI command history with Up/Down arrow navigation, customizable EOL line endings (LF, CR, CRLF), local echo toggles, and one-click macro buttons (*IDN?, :SYST:INFO, *RST).  Architecture & Hardware CommunicationThe application uses a 3-tier hardware abstraction architecture:+-----------------------------------------------------------------------+
|                       Web Frontend (Browser)                          |
|   HTML5 Dashboard  |  3D Joystick Canvas  |  NVRAM Table / Wizard     |
+-----------------------------------------------------------------------+
                                   |
                       WebSerial API (115200 Baud)
                                   |
+-----------------------------------------------------------------------+
|                 Bridge Microcontroller (CDC-ACM)                       |
|   Executes SCPI commands (e.g., :SPI:WriteReaD, :SPI:CS0)[cite: 1, 3]    |
+-----------------------------------------------------------------------+
                                   |
                       4-Wire SPI Bus + CS0[cite: 1, 3]
                                   |
+-----------------------------------------------------------------------+
|                    MLX90396 3D Hall Sensor                            |
|   Measures Bx, By, Bz magnetic field & die temperature[cite: 1, 3]     |
+-----------------------------------------------------------------------+
SCPI Commands Used by Bridge Firmware:SPI:Init 0 — Initialize primary SPI peripheral.  :CON:CS1:GPIO:INIT:OUT 0 — Configure Chip Select pin as GPIO output.  :SPI:BUFfer 1,1,1,1,0 — Configure buffer parameters.  :VDD:3V3 / :VDD:OFF — Power rail control[cite: 1].:SPI:CS0 0 / :SPI:CS0 1 — Manual CS line assertion and de-assertion.  :SPI:WriteReaD <bytes> — Full-duplex SPI transfer of comma-separated decimal bytes.  File StructurePlaintextMLX90396-WebUI/
├── index.html        # Main HTML structure, styles, SVG assets, and view tabs[cite: 2]
├── app.js            # Main application controller, UI event bindings, WebSerial manager[cite: 1]
├── mlx_api.js        # MLX90396 device class, command builder, and CRC-8 calculator
└── resources/
    └── background.jpg# UI background image asset[cite: 2]
Sensor API Command Reference (mlx_api.js)All SPI transactions to the MLX90396 use a custom CRC-8 calculation (Polynomial 0x2F, Initial value 0xFF, Final XOR 0xFF):  $$\text{Polynomial: } x^8 + x^5 + x^3 + x^2 + x + 1 \quad (0\text{x}2\text{F})$$  OpcodeCommandDescriptionSPI Frame Structure0xF0  RTSoft Reset[0xF0, CRC]  0xE0  HSMemory Store (RAM $\rightarrow$ NVRAM)[0xE0, CRC]  0xD0  HRMemory Recall (NVRAM $\rightarrow$ RAM)[0xD0, CRC]  0x80  EXExit Mode / Enter Idle[0x80 | Mode, CRC]  0x50  RRRead Register[0x50, Reg, CRC]  0x60  WRWrite Register[0x60, Reg, Data_H, Data_L, CRC]  0x10  SBStart Burst Mode[0x10 | Mask_H, Mask_M, Mask_L, CRC]  0x30  SMStart Single Measurement[0x30 | Mask_H, Mask_M, Mask_L, CRC]  0x40  RM_NO_TEMPRead Measurement (XYZ)[0x40, CRC]  0x41  RM_TEMPRead Measurement (XYZ + Temp)[0x41, CRC][cite: 3]Getting StartedPrerequisitesA Chromium-based browser supporting the WebSerial API:Google Chrome (v89+)Microsoft Edge (v89+)Opera (v75+)A connected USB-to-Serial bridge device programmed with SCPI firmware.Note: The WebSerial API requires a secure context. The site must be served over https:// or hosted locally at http://localhost.  Running LocallyClone the repository:Bashgit clone https://github.com/YOUR_USERNAME/MLX90396-WebUI.git
cd MLX90396-WebUI
Serve the app using any static web server:Using Python:Bashpython -m http.server 8000
Using Node.js serve:Bashnpx serve .
Open in browser:Navigate to http://localhost:8000 or http://localhost:3000.Connect to hardware:
Click the Connect USB button at the top right, select your USB/Serial COM port from the browser permission prompt, and set the baud rate (default is 115200).  LicenseDistributed under the MIT License. See LICENSE for more information.
